window.WCU_CONFIG = window.WCU_CONFIG || {
  // Leave this empty when the website and API are served from the same domain.
  // Set a full origin only if you later separate the frontend and backend.
  apiBaseUrl: ""
};
